{
  "name": "@voxel-02/monorepo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "build:deps": "npm run build -w @voxel-02/core && npm run build -w @voxel-02/protocol",
    "dev:client": "npm run dev -w @voxel-02/client",
    "dev:server": "npm run build:deps && npm run dev -w @voxel-02/server",
    "clean": "rm -rf packages/*/dist packages/*/node_modules node_modules"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
