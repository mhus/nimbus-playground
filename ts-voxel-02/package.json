{
  "name": "@voxel-02/monorepo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev:client": "npm run dev -w @voxel-02/client",
    "dev:server": "npm run dev -w @voxel-02/server",
    "clean": "rm -rf packages/*/dist packages/*/node_modules node_modules"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
